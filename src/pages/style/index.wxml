<view class="container">
    <view class="list">
        <block wx:for="{{list}}" wx:key="unique">
            <view class="image">
                <image src="{{item.image_url[0]}}" />
            </view>
        </block>
    </view>
</view>